# Generate real secret values, e.g.:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MASTER_KEY=  # 64 hex chars (32 bytes) - AES-256-GCM master key for the key vault
COOKIE_SECRET=  # random hex string - signs session cookies
PORT=8123
NODE_NO_WARNINGS=1
